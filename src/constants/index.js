import { Dimensions } from 'react-native';

const WIDTH = Dimensions.get('screen').width;
const HEIGHT = Dimensions.get('screen').height;
const BACKGROUND_COLOR = '#222730';
const SIZE = 16;
const COLOR = 'white';

export {
    WIDTH,
    HEIGHT,
    BACKGROUND_COLOR,
    SIZE,
    COLOR
};